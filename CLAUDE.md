# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## リポジトリの目的

READMEによれば「AI-powered Japanese stock screener」（AIを活用した日本株スクリーナー）の
リポジトリ。ただし現時点では**初期コミットのみのグリーンフィールド状態**であり、
リポジトリに含まれるのは`README.md`（1行の説明文）だけである。

## 現在の状態（2026-07-20時点）

- ソースコード: なし
- ビルドシステム / 依存関係定義: なし
- テスト / リンター: なし
- CI（GitHub Actions等）: なし

したがって「実行すべきコマンド」は現時点で存在しない。コードを追加する際は、言語・
ツールチェーン・ディレクトリ構成を最初の実装者（そのセッション）が決めることになる。

## 関連リポジトリ（同一アカウント内）

同じアカウント（shinya25251015）には日本株投資に関する関連リポジトリが存在する:

- **`Future100`** — 2035年に社会インフラとして不可欠になる日本企業を発掘・積立する
  個人投資プロジェクトの運用支援リポジトリ。日次監視の自動化（yfinance + GitHub Actions）と
  基礎データ（`config/*.yaml`）を保持。
- **`zensu-chosa`** — 決算サプライズシグナル（PEAD）検証の生データと結果を格納した
  データリポジトリ。
- **`jp-stock-screener`** — `zensu-chosa`から参照されている既存のスクリーナー
  （テクニカル・ファンダメンタルズ・決算サプライズの3軸評価を持つ）。

**注意**: 本リポジトリ（AI-Stock-Screener）とこれらのリポジトリ、特に既存の
`jp-stock-screener`との関係（後継・移行先・別プロジェクト等）はどこにも文書化されて
いない。関係を推測してコードや前提を持ち込まず、ユーザーに確認してから作業すること
（Future100の§8「一次情報主義」——二次情報や推測の「〜のはず」を採用しない——という
アカウント全体の運用方針に従う）。

## このリポジトリで作業する際の指針

- **最初のコード追加時にこのCLAUDE.mdを必ず更新すること。** セットアップコマンド・
  テスト実行方法・アーキテクチャ・設計判断を追記する。現状の「グリーンフィールド」
  記述は実装が入った時点で古くなる。
- ドキュメント（CLAUDE.md含む）は日本語で書く。関連リポジトリのCLAUDE.md・README・
  コミットメッセージはすべて日本語で運用されている。
- 株価・財務データを扱う場合、関連リポジトリで判明済みの知見に留意する:
  yfinanceの`priceToBook`/`returnOnEquity`は銘柄によって実態と大幅に乖離することがある
  （検証記録: `Future100/docs/verification_2026-07-17.md`）。また開発サンドボックス環境は
  ネットワークポリシー上、外部金融データへの直接アクセスがブロックされていることが多い。
  ロジック検証はモックデータで行い、実データでの確認はGitHub Actionsまたはユーザーの
  ローカル環境に委ねるのが既存の運用パターン。
